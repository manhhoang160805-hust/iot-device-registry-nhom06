# QUY TẤC ĐÓNG GÓP 
1. Không push trực tiếp lên master
2. mỗi nhiệm vụ dùng một branch riêng.
3. commit phải ghi rõ nội dung thay đổi.
4. Tạo pull request trước khi merge.
5. chỉ merge sau khi đã review.
6. Sau khi merge phải pull code mới nhất.
